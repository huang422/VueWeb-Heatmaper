# 🚀 Quick Start Guide

## 快速啟動指南

### 方法 1: 使用啟動腳本（最簡單）

#### 1. 啟動後端（終端 1）
```bash
cd /home/user/Desktop/Tom/store_heatmap
./start_backend.sh
```

#### 2. 啟動前端（終端 2）
```bash
cd /home/user/Desktop/Tom/store_heatmap
./start_frontend.sh
```

#### 3. 打開瀏覽器
訪問: **http://localhost:5173**

---

### 方法 2: 手動啟動

#### 1. 啟動後端
```bash
cd /home/user/Desktop/Tom/store_heatmap/backend
~/anaconda3/envs/fapi/bin/python -m src.main
```

#### 2. 啟動前端（新終端）
```bash
cd /home/user/Desktop/Tom/store_heatmap/frontend
npm run dev
```

---

## ✅ 驗證安裝

### 檢查後端
```bash
curl http://localhost:8000/health
# 應返回: {"status":"ok","timestamp":"..."}
```

### 檢查 API 文檔
瀏覽器訪問: **http://localhost:8000/docs**

### 檢查前端
瀏覽器訪問: **http://localhost:5173**

---

## 🎯 功能測試清單

啟動應用後，測試以下功能：

### ✅ 地圖顯示
- [ ] 台灣地圖正確顯示
- [ ] 熱力圖點位正常顯示
- [ ] 滑鼠懸停顯示 tooltip

### ✅ 時間控制
- [ ] 點擊 "播放" 按鈕開始自動播放
- [ ] 每 3 秒自動切換小時（0-23）
- [ ] 點擊 "暫停" 按鈕停止播放
- [ ] 點擊 "重置" 按鈕回到 00:00
- [ ] 拖動時間軸滑桿手動選擇小時
- [ ] 手動操作時自動暫停播放

### ✅ 篩選控制
- [ ] 月份選擇器（4 個月份按鈕）
  - 2024年12月
  - 2025年2月
  - 2025年5月
  - 2025年8月
- [ ] 停留時長指標選擇
  - 全部停留人數 👥
  - 停留10分鐘以下 ⚡
  - 停留10-30分鐘 ⏱️
  - 停留30分鐘以上 🕐

### ✅ 人口統計圖表
- [ ] 性別分布餅圖（男性/女性）
- [ ] 年齡分布柱狀圖（10 個年齡組）
- [ ] 圖表隨月份/小時自動更新

### ✅ 數據統計
- [ ] 顯示數據點數
- [ ] 顯示最小值/最大值

---

## 🐛 常見問題

### 問題 1: 後端無法啟動
**錯誤**: `ModuleNotFoundError: No module named 'fastapi'`

**解決方案**:
```bash
cd backend
~/anaconda3/envs/fapi/bin/pip install -r requirements.txt
```

### 問題 2: 前端無法啟動
**錯誤**: `Cannot find module 'vue'`

**解決方案**:
```bash
cd frontend
npm install
```

### 問題 3: 端口已被佔用
**錯誤**: `Port 8000 is already in use`

**解決方案**:
```bash
# 查找佔用端口的進程
lsof -i :8000
# 終止進程
kill -9 <PID>
```

### 問題 4: CORS 錯誤
確保後端和前端都在運行，並且前端通過 Vite 代理訪問後端。

---

## 📊 API 端點

### Health Check
```bash
GET http://localhost:8000/health
```

### Metadata
```bash
GET http://localhost:8000/api/metadata
```

### Heatmap Data
```bash
GET http://localhost:8000/api/heatmap?month=202412&hour=0&metric=avg_total_users
```

### Demographics
```bash
GET http://localhost:8000/api/demographics?month=202412&hour=0&metric=avg_total_users
```

---

## 🎨 開發工具

### API 文檔
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

### 開發伺服器
- 後端: http://localhost:8000
- 前端: http://localhost:5173

---

## 🔄 重新啟動

### 重啟後端
1. 按 `CTRL+C` 停止後端
2. 重新運行 `./start_backend.sh`

### 重啟前端
1. 按 `CTRL+C` 停止前端
2. 重新運行 `./start_frontend.sh`

---

## 📝 下一步

- 測試所有功能
- 檢查控制台是否有錯誤
- 驗證數據正確性
- 測試響應式設計（調整瀏覽器大小）

---

## 🎉 完成！

如果一切正常，你應該看到：
- ✅ 台灣地圖與熱力圖
- ✅ 自動播放控制
- ✅ 時間軸滑桿
- ✅ 月份和指標選擇器
- ✅ 性別和年齡圖表
- ✅ 實時數據統計

享受你的互動式地理熱力圖可視化系統！🗺️
